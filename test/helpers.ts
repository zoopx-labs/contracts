/*
 * test/helper.ts
 * This file is typically used to load common test setup logic.
 * You can safely put global test imports here.
 */

import "@nomicfoundation/hardhat-chai-matchers";

// If you need to define any reusable functions or constants, do that below.
// For example:
export const HOURS = (n: number) => n * 60 * 60;

// This file should be imported **once** at the top of each test file (or in test setup).
// For example, in your test file:
// import "./helpers";
